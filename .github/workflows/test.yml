name: playing with matrix
on:
  push:
    paths:
      - 's3/**'
    
jobs:
  build:
    strategy:
      max-parallel: 1
      matrix:
        include:
          - environment: testing
            runner: ubuntu-latest
          - environment: production
            runner: ubuntu-18.04
    runs-on: [ '${{matrix.runner}}']
    steps:
    - uses: actions/checkout@v1
    - name: run the workflow  
      run: echo 'running on test and production'
